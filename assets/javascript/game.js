var countdown = 30;
var questionArray = []; // TODO: Insert list of questions
var correct = 0;
var incorrect = 0;
var questionNum = 1;

// Constructor for Trivia Questions

function makeQuestion(prompt, answer, image) {
    this.prompt = currentQuestion;
    this.answer = currentAnswer;
    this.image = currentImage;
}

var questions = [
    {prompt: "How many days are in a week?", answer: "7", image: "[INSERT IMG SRC HERE]"},
    {prompt: "True or False"}
]